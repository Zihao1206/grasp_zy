# RM65 机械臂视觉抓取系统使用说明书

> 本文档面向企业现场操作人员，无需编程基础即可按步骤完成系统部署与日常使用。

---

## 目录

- [第1章 系统概述](#第1章-系统概述)
- [第2章 硬件连接与准备](#第2章-硬件连接与准备)
- [第3章 网络配置](#第3章-网络配置)
- [第4章 软件环境安装](#第4章-软件环境安装)
- [第5章 参数配置指南（核心章节）](#第5章-参数配置指南核心章节)
- [第6章 标定指引（简化版）](#第6章-标定指引简化版)
- [第7章 系统启停与CTU通信](#第7章-系统启停与ctu通信)
- [第8章 物品类别扩展](#第8章-物品类别扩展)
- [第9章 故障排查](#第9章-故障排查)
- [附录A 配置文件速查表](#附录a-配置文件速查表)
- [附录B 设备IP速查表](#附录b-设备ip速查表)
- [附录C 相关文档索引](#附录c-相关文档索引)

---

## 第1章 系统概述

### 1.1 系统做什么

本系统是一套"会看、会抓、会放"的智能机器人分拣方案。简单理解，它的工作流程如下：

1. **小车到达**：CTU（智能搬运小车）载着货箱移动到机械臂工位
2. **识别物品**：相机拍下货箱照片，人工智能（AI）模型识别出里面有什么物品、每个物品在哪里
3. **机械臂抓取**：系统计算出最佳抓取位置和角度，控制机械臂伸过去、夹住物品
4. **放到指定位置**：机械臂把物品搬运到旁边的收集箱里
5. **重复直到清空**：反复执行，直到货箱中目标物品全部被抓完

### 1.2 硬件清单

| 名称 | 型号/规格 | 数量 | 作用 |
|------|-----------|------|------|
| 机械臂 | RM65（睿尔曼） | 1台 | 执行抓取和放置动作 |
| 相机 | Intel RealSense D435/D435i | 1个 | 拍摄彩色图和深度图 |
| 开发板 | Jetson Orin NX 8GB | 1块 | 运行AI识别程序和机械臂控制程序 |
| CTU小车 | 带货叉的潜伏式AGV | 1台 | 搬运货箱到抓取工位 |
| 网线 | 普通以太网线 | 3根 | 连接开发板、机械臂、CTU/MOXA |
| USB线 | USB 3.0 | 1根 | 连接相机和开发板 |
| 夹爪 | 电动夹爪（Modbus协议） | 1个 | 安装在机械臂末端，负责夹取物品 |
| 显示器与键鼠 | HDMI + USB | 1套 | 首次配置开发板时使用 |

### 1.3 两个核心文件的分工

系统中真正干活的只有两个文件，其他都是辅助：

| 文件 | 作用 | 通俗解释 |
|------|------|---------|
| `grasp_zy_zhiyuan1215.py` | 机械臂抓取的核心逻辑 | 负责"看照片、算位置、动手抓" |
| `ctu_conn.py` | CTU小车通信服务 | 负责"听小车的指令、汇报进度" |

整个系统的运行链路是：

`RunGraspd.service（系统服务）` → 启动 `ctu_conn.py` → 创建 `Grasp` 对象（连接硬件） → 机械臂、相机、AI模型全部就绪 → 等待CTU发来分拣指令

---

## 第2章 硬件连接与准备

### 2.1 连接拓扑

所有设备之间的连接关系如下图所示：

```
        Jetson Orin NX 开发板
        /          |          \
    网线        USB 3.0      网线
      /            |            \
RM65机械臂    RealSense相机    MOXA串口服务器
                                 |
                              CTU小车
```

**网线连接说明：**

1. 开发板的第一个网口 → 机械臂控制器（通过交换机或直接相连）
2. 开发板的第二个网口 → MOXA串口服务器（MOXA再连CTU小车的主控）

**USB连接说明：**

1. 开发板的USB 3.0口 → RealSense相机（必须使用USB 3.0，否则相机无法正常工作）

### 2.2 机械臂上电步骤

1. 确认机械臂底座电源线已插好
2. 打开机械臂控制器背后的电源开关
3. 等待约30秒，听到机械臂控制器发出"滴"声，表示启动完成
4. 观察机械臂底座指示灯，绿色常亮表示正常

**上电后必须确认：** 机械臂六个关节都可以手动轻微推动（如果推不动，说明抱闸未释放，需要检查电源）。

### 2.3 相机安装说明

1. 将RealSense相机固定在机械臂底座前方的支架上
2. 相机镜头朝下，对准机械臂的工作区域（也就是CTU小车货叉上的货箱位置）
3. 相机距离货箱表面约0.5米至0.7米
4. 用USB 3.0线连接到开发板
5. 确认相机上的指示灯亮起（白色或蓝色）

**安装完成后，相机位置一旦固定就不要再移动。** 如果移动了相机，必须重新执行第6章的"手眼标定"流程。

### 2.4 开发板上电

1. 给Jetson Orin NX开发板接上电源适配器
2. 开发板会自动开机，约60秒后系统启动完成
3. 如果是首次使用，需要接上显示器和键鼠进行配置

---

## 第3章 网络配置

### 3.1 所有设备IP对照表

现场所有设备都在同一个局域网内，IP地址段为 `192.168.127.x`：

| 设备 | IP地址 | 端口 | 说明 |
|------|--------|------|------|
| Jetson开发板（有线网口） | 192.168.127.102 | - | 运行抓取程序的主机 |
| Jetson开发板（无线WiFi） | 192.168.2.51 | - | 备用，用于SSH远程登录 |
| RM65机械臂 | 192.168.127.101 | 8080 | TCP/IP（网络通信协议）端口 |
| CTU小车（通过MOXA） | 192.168.127.253 | 8899 | TCP（传输控制协议）端口 |

### 3.2 网络连通验证方法

在开发板上打开终端，依次执行以下命令验证网络是否连通：

```bash
# 1. 验证开发板能否连上机械臂
ping 192.168.127.101

# 2. 验证开发板能否连上CTU/MOXA
ping 192.168.127.253
```

如果看到类似以下输出，说明网络正常：

```
64 bytes from 192.168.127.101: icmp_seq=1 ttl=64 time=0.5 ms
```

如果看到 `Destination Host Unreachable` 或 `请求超时`，说明网线没插好或IP地址配置有误。

### 3.3 如何修改IP地址

**如果现场网络环境不同（例如IP段不是192.168.127.x），需要修改以下两个文件：**

#### 修改机械臂IP

打开文件 `grasp_zy_zhiyuan1215.py`，找到第46行：

```python
self.robot_handle = self.robot.rm_create_robot_arm("192.168.127.101", 8080)
```

将 `"192.168.127.101"` 改成现场实际的机械臂IP地址。

#### 修改CTU IP

打开文件 `ctu_conn.py`，找到第26行：

```python
def __init__(self, grasp, server_ip='192.168.127.253', port=8899):
```

将 `'192.168.127.253'` 改成现场实际的CTU/MOXA IP地址。第163行也有一个相同的服务器地址设置，两处保持一致即可。

### 3.4 MOXA配置简述

MOXA是一个"串口转网口"的设备，作用是让开发板通过网络与CTU小车通信。

**MOXA出厂默认配置：**

- 登录地址：在浏览器输入 `192.168.127.253`
- 默认用户名：`admin`
- 默认密码：`moxa`

**配置步骤（仅首次部署或更换MOXA时需要）：**

1. 用网线将电脑与MOXA直连
2. 将电脑网卡IP临时改为 `192.168.127.x`（x不能是253）
3. 浏览器访问 `192.168.127.253`，输入用户名密码登录
4. 导入MOXA配置文件（从项目 `media/` 目录中获取）
5. 确认工作模式为 "TCP Server"，端口为 `8899`
6. 保存并重启MOXA

如需更详细的MOXA配置图文步骤，请参考 `CTUArm.md` 第3节。

---

## 第4章 软件环境安装

### 4.1 Conda环境创建

Conda是一个Python环境管理工具，类似于给程序提供一个"独立房间"，避免不同项目之间的依赖冲突。

**操作步骤：**

1. 打开开发板终端
2. 创建名为 `zy_torch` 的环境：

```bash
conda create -n zy_torch python=3.8 -y
```

3. 激活环境：

```bash
conda activate zy_torch
```

### 4.2 依赖安装

项目根目录下有一个 `requirements.txt` 文件，里面列出了所有需要的Python库。在激活 `zy_torch` 环境后执行：

```bash
cd /home/jet/zoneyung/grasp_static
pip install -r requirements.txt
```

主要安装的依赖包括：

| 依赖名称 | 用途 |
|----------|------|
| torch / torchvision | 深度学习框架（PyTorch） |
| mmdet / mmcv / mmengine | 目标检测模型框架 |
| opencv-python | 图像处理 |
| pyrealsense2 | RealSense相机驱动 |
| numpy / scipy / scikit-image | 科学计算 |
| transforms3d | 三维坐标变换 |

### 4.3 机械臂SDK安装

1. 将机械臂厂商提供的SDK文件夹 `robotic_arm_package` 放到项目根目录下
2. 确认SDK目录下有 `rm_robot_interface.py` 和 `rm_ctypes_wrap.py` 等文件
3. SDK不需要额外的 `pip install`，只要文件夹位置正确即可

### 4.4 项目文件部署目录

**推荐部署路径：**

```
/home/jet/zoneyung/grasp_static/
```

该路径与系统服务 `RunGraspd.service` 中配置的路径一致。如果实际目录不同，需要修改服务文件中的路径（详见第7章）。

**项目根目录下必须包含以下文件夹：**

```
grasp_static/
├── grasp_zy_zhiyuan1215.py   # 核心抓取逻辑
├── ctu_conn.py               # CTU通信
├── config.py                 # 全局配置
├── camera.py                 # 相机驱动
├── gripper_zhiyuan.py        # 夹爪控制
├── requirements.txt          # 依赖列表
├── RunGraspd.service         # 系统服务配置
├── dataset/                  # 数据集目录
├── models/                   # AI模型文件
│   ├── weights/
│   ├── mmdetection/
│   └── gqcnn_server/
├── doc/                      # 文档
│   └── single_new.txt        # 模型基因描述文件
└── outputs/                  # 输出图片（自动创建）
```

### 4.5 安装验证命令

全部安装完成后，依次执行以下验证：

```bash
# 1. 进入项目目录
cd /home/jet/zoneyung/grasp_static

# 2. 激活环境
conda activate zy_torch

# 3. 验证Python环境
python -c "import torch; print(torch.cuda.is_available())"
# 预期输出：True（表示GPU可用）

# 4. 验证相机
python camera.py
# 预期结果：弹出相机画面窗口，按q退出

# 5. 验证机械臂连接（手动模式下）
python -c "from grasp_zy_zhiyuan1215 import Grasp; g=Grasp(hardware=True)"
# 预期结果：机械臂回零到初始姿态，无报错
```

---

## 第5章 参数配置指南（核心章节）

### 5.1 参数总览表

本系统涉及的配置参数分散在多个文件中。下表按"修改必要性"分为三级：

| 修改必要性 | 含义 |
|-----------|------|
| **必须修改** | 换现场后几乎肯定要改，否则系统无法运行 |
| **可能需要修改** | 根据实际部署情况决定是否调整 |
| **一般不改** | 除非有明确的调试需求，否则保持默认值 |

#### 参数速查总表

| 参数名 | 所在文件 | 行号 | 当前值 | 修改必要性 | 何时需要修改 |
|--------|---------|------|--------|-----------|-------------|
| 机械臂IP | `grasp_zy_zhiyuan1215.py` | 46 | `"192.168.127.101"` | 必须修改 | 机械臂IP地址变化时 |
| 机械臂端口 | `grasp_zy_zhiyuan1215.py` | 46 | `8080` | 一般不改 | - |
| CTU IP | `ctu_conn.py` | 26, 163 | `"192.168.127.253"` | 必须修改 | CTU/MOXA IP变化时 |
| CTU端口 | `ctu_conn.py` | 26 | `8899` | 一般不改 | - |
| Tcam2base矩阵 | `grasp_zy_zhiyuan1215.py` | 66-71 | 4x4矩阵 | 必须修改 | 相机位置移动后 |
| Tcam2base矩阵（备用） | `config.py` | 4-8 | 4x4矩阵 | 必须修改 | 同上，两处都要改 |
| 相机内参矩阵 | `camera.py` | 38-42 | 3x3矩阵 | 可能需要修改 | 更换相机型号后 |
| 图像裁剪范围 | `grasp_zy_zhiyuan1215.py` | 385-386 | `[:, 80:560]` | 可能需要修改 | 工作区域边界调整时 |
| init_pose | `grasp_zy_zhiyuan1215.py` | 56 | `[86,-129,127,-0.8,71,-81]` | 可能需要修改 | 机械臂初始姿态需调整 |
| mid_pose | `grasp_zy_zhiyuan1215.py` | 57 | `[0,-129,127,-0.7,71,-81]` | 可能需要修改 | 中间过渡姿态需调整 |
| mid_pose1 | `grasp_zy_zhiyuan1215.py` | 58 | `[0,-129,80,-0.7,100,-81]` | 可能需要修改 | 抓取准备姿态需调整 |
| lift2init_pose | `grasp_zy_zhiyuan1215.py` | 61 | `[65,-129,127,-0.7,77,1]` | 可能需要修改 | 回初始姿态路径需调整 |
| place_mid_pose | `grasp_zy_zhiyuan1215.py` | 62 | `[65,-129,60,0,121,1]` | 可能需要修改 | 放置路径需调整 |
| place_mid_pose2 | `grasp_zy_zhiyuan1215.py` | 63 | `[69,-129,60,0,9,1]` | 可能需要修改 | 放置路径需调整 |
| place_last_pose | `grasp_zy_zhiyuan1215.py` | 64 | `[69,-104,38,-2,9,1]` | 可能需要修改 | 最终放置位置需调整 |
| place_last_pose（备用） | `config.py` | 1 | `[77,-104,38,-2,9,1]` | 可能需要修改 | 同上，优先改主文件 |
| Z轴高度限制 | `config.py` | 36-37 | `pose2=0.540` | 可能需要修改 | 防止碰撞的安全高度 |
| slope_angle | `grasp_zy_zhiyuan1215.py` | 300 | `np.pi/8` | 一般不改 | 边缘倾斜补偿角度 |
| 边缘范围 | `grasp_zy_zhiyuan1215.py` | 301-303 | 左80,右480,上120,下292 | 可能需要修改 | 工作区域边界变化 |
| t_tcp_flange | `grasp_zy_zhiyuan1215.py` | 296 | `[0,0,0.2]` | 一般不改 | 夹爪安装偏移补偿 |
| tcp_compensate | `grasp_zy_zhiyuan1215.py` | 297 | `[0,0,0.018]` | 一般不改 | TCP末端补偿 |
| robot_speed | `grasp_zy_zhiyuan1215.py` | 39 | `30` | 可能需要修改 | 调整机械臂运动速度 |
| robot_speed（备用） | `config.py` | 25, 34 | `20` | 一般不改 | 备用默认值 |
| 碰撞等级 | `grasp_zy_zhiyuan1215.py` | 47 | `5` | 一般不改 | 碰撞灵敏度 |
| 最大抓取次数 | `ctu_conn.py` | 116 | `count + 5` | 一般不改 | 防死循环保护 |
| 最大逆解失败 | `ctu_conn.py` | 120 | `3` | 一般不改 | 防死循环保护 |
| 抓取模型权重 | `grasp_zy_zhiyuan1215.py` | 84 | `models/test_250927...` | 一般不改 | 更换模型时 |
| 检测模型配置 | `grasp_zy_zhiyuan1215.py` | 86 | `models/mmdetection/...` | 一般不改 | 更换模型时 |
| 检测模型权重 | `grasp_zy_zhiyuan1215.py` | 87 | `models/weights/...` | 一般不改 | 更换模型时 |
| 基因描述文件 | `grasp_zy_zhiyuan1215.py` | 76 | `doc/single_new.txt` | 一般不改 | 模型结构变化时 |

### 5.2 网络参数

网络参数决定了系统能否找到机械臂和CTU小车。**换到现场后，第一件事就是确认这些IP地址是否正确。**

#### 机械臂IP和端口

- **文件：** `grasp_zy_zhiyuan1215.py`
- **行号：** 第46行
- **当前值：** `"192.168.127.101"`, `8080`
- **修改方法：** 用文本编辑器打开文件，将双引号里的IP地址替换为现场实际的机械臂IP

#### CTU IP和端口

- **文件：** `ctu_conn.py`
- **行号：** 第26行（默认值），第163行（实际连接用）
- **当前值：** `'192.168.127.253'`, `8899`
- **修改方法：** 修改第26行的 `server_ip` 默认值即可

#### 碰撞等级

- **文件：** `grasp_zy_zhiyuan1215.py`
- **行号：** 第47行
- **当前值：** `5`
- **说明：** 数值范围0到8。0表示关闭碰撞检测，8表示最灵敏。当前设为5，属于中等灵敏度。如果机械臂频繁误报碰撞，可以调低到3或4；如果需要更高的安全保护，可以调高到6或7。

### 5.3 相机参数

#### Tcam2base（相机到机械臂基座的坐标变换矩阵）

- **文件：** `grasp_zy_zhiyuan1215.py` 第66-71行
- **备用定义：** `config.py` 第4-8行
- **当前值：** 一个4行4列的矩阵（详见文件）
- **何时修改：** 相机被移动、重新安装或更换支架后
- **如何获取新值：** 执行第6章的手眼标定流程

**重要提示：** `grasp_zy_zhiyuan1215.py` 和 `config.py` 两处都有Tcam2base的定义。程序运行时实际使用的是 `grasp_zy_zhiyuan1215.py` 中的值，但建议两处保持一致，避免后续维护时混淆。

#### 相机内参矩阵

- **文件：** `camera.py`
- **行号：** 第38-42行
- **当前值：**

```python
self.intr = np.array([
    [604.335, 0, 316.187],
    [0, 604.404, 248.611],
    [0, 0, 1]
])
```

- **何时修改：** 更换了不同型号的RealSense相机，或者同一台相机重新标定后
- **如何获取新值：** 执行第6章的相机内参标定，得到新的焦距（fx, fy）和光心（cx, cy）填入对应位置

#### 图像裁剪范围

- **文件：** `grasp_zy_zhiyuan1215.py`
- **行号：** 第385-386行
- **当前值：** `[:, 80:560]`（表示从第80列到第560列，裁剪掉左右边缘）
- **何时修改：** 如果货箱在画面中的位置变了，或者想去掉更多干扰区域
- **修改方法：** 调整两个数字。80表示左边裁掉80个像素，560表示右边保留到560像素（原始宽度640，所以右边裁掉80个像素）

### 5.4 机械臂位姿

位姿（Pose）就是机械臂各个关节的角度。系统预设了7个关键位姿，构成一条完整的"抓取流水线"。

每个位姿都是6个数字，分别代表机械臂6个关节的角度（单位：度）：

| 位姿名称 | 文件 | 行号 | 当前值 | 含义 |
|---------|------|------|--------|------|
| init_pose | `grasp_zy_zhiyuan1215.py` | 56 | `[86, -129, 127, -0.8, 71, -81]` | 初始等待姿态 |
| mid_pose | `grasp_zy_zhiyuan1215.py` | 57 | `[0, -129, 127, -0.7, 71, -81]` | 中间过渡姿态 |
| mid_pose1 | `grasp_zy_zhiyuan1215.py` | 58 | `[0, -129, 80, -0.7, 100, -81]` | 抓取准备姿态 |
| lift2init_pose | `grasp_zy_zhiyuan1215.py` | 61 | `[65, -129, 127, -0.7, 77, 1]` | 放置后回初始姿态 |
| place_mid_pose | `grasp_zy_zhiyuan1215.py` | 62 | `[65, -129, 60, 0, 121, 1]` | 放置过程姿态1 |
| place_mid_pose2 | `grasp_zy_zhiyuan1215.py` | 63 | `[69, -129, 60, 0, 9, 1]` | 放置过程姿态2 |
| place_last_pose | `grasp_zy_zhiyuan1215.py` | 64 | `[69, -104, 38, -2, 9, 1]` | 最终放置姿态 |
| place_last_pose（备用） | `config.py` | 1 | `[77, -104, 38, -2, 9, 1]` | 同上（优先改主文件） |

**位姿修改注意事项：**

1. 这些位姿值是在特定现场调试出来的，换到现场后很可能需要微调
2. 修改前务必理解每个位姿的作用：目的是让机械臂在运动中不会撞到周围的货架、小车或自身
3. 建议每次只改一个值，改完后手动测试机械臂运动轨迹
4. 第4个值很小（如-0.8），这是弧度制的角度，**不要改成很大的数**

#### Z轴高度限制

- **文件：** `config.py`
- **行号：** 第36-37行
- **当前值：** `pose2_2 = 0.540`, `pose2 = 0.540`
- **说明：** 这两个值限制了机械臂末端下降的最高位置（单位：米）。0.540米约等于54厘米。如果货箱高度变了，或者抓取平面高度变了，需要相应调整这两个值。在 `grasp_zy_zhiyuan1215.py` 第481-482行还有0.534的硬限制。

### 5.5 抓取补偿参数

这些参数用于修正抓取时的位置和角度偏差，属于"精细微调"级别。

| 参数名 | 文件 | 行号 | 当前值 | 含义 |
|--------|------|------|--------|------|
| t_tcp_flange | `grasp_zy_zhiyuan1215.py` | 296 | `[0, 0, 0.2]` | 夹爪安装位置到末端的距离补偿（0.2米） |
| tcp_compensate | `grasp_zy_zhiyuan1215.py` | 297 | `[0, 0, 0.018]` | 末端Z方向微调补偿（0.018米，约1.8厘米） |
| slope_angle | `grasp_zy_zhiyuan1215.py` | 300 | `np.pi/8` | 边缘区域倾斜补偿角度（约22.5度） |
| 边缘左边界 | `grasp_zy_zhiyuan1215.py` | 301 | `column_left=80` | 左侧边缘判定线（像素） |
| 边缘右边界 | `grasp_zy_zhiyuan1215.py` | 301 | `column_right=480` | 右侧边缘判定线（像素） |
| 边缘上边界 | `grasp_zy_zhiyuan1215.py` | 303 | `row_up=120` | 上侧边缘判定线（像素） |
| 边缘下边界 | `grasp_zy_zhiyuan1215.py` | 303 | `row_down=292` | 下侧边缘判定线（像素） |

**补偿参数通俗解释：**

- **t_tcp_flange：** 夹爪装在机械臂末端上，中间有一段距离。这个参数告诉系统"夹爪实际比末端低20厘米"，这样系统计算抓取点时就会把这段距离考虑进去。
- **tcp_compensate：** 额外的一点点高度修正（1.8厘米），用于应对实际安装中的微小偏差。
- **slope_angle：** 当物品靠近货箱边缘时，机械臂末端会微微倾斜一个角度（约22.5度），这样夹爪可以从斜上方伸进去，避免碰到货箱侧壁。
- **边缘范围（column_left/right, row_up/down）：** 定义了"边缘区域"的边界。像素坐标落在这个范围之外的物品，会被认为是"在边缘"，从而触发倾斜补偿。

### 5.6 模型路径配置

AI模型文件的路径在 `grasp_zy_zhiyuan1215.py` 中配置：

| 模型 | 文件 | 行号 | 当前路径 |
|------|------|------|---------|
| 抓取模型权重 | `grasp_zy_zhiyuan1215.py` | 84 | `models/test_250927_1644__zoneyung_/epoch_84_accuracy_1.00` |
| 检测模型配置 | `grasp_zy_zhiyuan1215.py` | 86 | `models/mmdetection/configs/myconfig_zy.py` |
| 检测模型权重 | `grasp_zy_zhiyuan1215.py` | 87 | `models/weights/epoch_20_last.pth` |
| 基因描述 | `grasp_zy_zhiyuan1215.py` | 76 | `doc/single_new.txt` |

**说明：**

1. 这些路径都是相对于项目根目录的相对路径
2. 只要项目目录结构没变，这些路径通常不需要修改
3. 如果重新训练了模型，得到了新的权重文件，只需要把新文件名替换到对应行即可
4. 基因描述文件 `doc/single_new.txt` 描述了神经网络的结构，与模型权重配套使用，不要随意更换

### 5.7 速度与安全参数

| 参数名 | 文件 | 行号 | 当前值 | 说明 |
|--------|------|------|--------|------|
| robot_speed | `grasp_zy_zhiyuan1215.py` | 39 | `30` | 机械臂运动速度（有效范围0-50） |
| robot_speed（备用） | `config.py` | 25, 34 | `20` | 备用默认值，当前未生效 |
| 最大抓取次数 | `ctu_conn.py` | 116 | `count + 5` | 比实际物品数多5次，防止遗漏 |
| 最大逆解失败 | `ctu_conn.py` | 120 | `3` | 连续3次算不出抓取姿势就放弃 |

**速度参数说明：**

- `robot_speed` 的有效范围是0到50。0表示静止不动，50表示最高速度。
- 当前设为30，属于中等偏快的速度。调试时建议先降到10-15，确认运动轨迹安全后再逐步提高。
- 注意：这个值不是百分比！30不代表30%速度，而是机械臂控制器内部的第30级速度。

**安全参数说明：**

- **最大抓取次数：** 假设CTU报告货箱里有5个目标物品，系统最多会尝试抓取10次（5+5）。这是为了防止某个物品被遮挡或识别不准导致无限循环。
- **最大逆解失败：** 如果系统连续3次都算不出机械臂该怎么动（逆运动学无解），就停止当前批次。这通常意味着物品位置太偏或太深，机械臂够不到。

---

## 第6章 标定指引（简化版）

标定就是"告诉系统相机和机械臂之间的位置关系"。标定做得好，机械臂才能准确抓到物品；标定不准，机械臂会抓偏甚至抓空。

### 6.1 相机内参标定

#### 什么是内参

相机内参描述的是相机本身的"光学特性"，主要包括：

- **焦距（fx, fy）：** 镜头有多"广角"
- **光心（cx, cy）：** 图像中心点在像素坐标系中的位置

同一个相机，只要焦距不调整，内参一般不需要重新标定。

#### 填在哪里

- **文件：** `camera.py`
- **行号：** 第38-42行

```python
self.intr = np.array([
    [604.335, 0, 316.187],      # fx, 0, cx
    [0, 604.404, 248.611],      # 0, fy, cy
    [0, 0, 1]                   # 最后一行固定
])
```

#### 推荐工具

1. 使用RealSense官方工具 `rs-enumerate-devices` 查看出厂内参
2. 使用OpenCV棋盘格标定法（需要打印一张棋盘格标定板）
3. 如果没有特殊精度要求，可以直接使用当前文件中的默认值，RealSense相机的出厂内参通常足够准确

### 6.2 手眼标定

#### 什么是手眼标定

手眼标定的目的是求出"相机坐标系"到"机械臂基座坐标系"的变换关系，也就是Tcam2base矩阵。

通俗地说：相机看到物品在自己的坐标系中的位置，机械臂在自己的坐标系中运动。手眼标定就是建立这两个坐标系之间的"翻译词典"，让机械臂知道"相机说的'左边10厘米'，在我的世界里是哪里"。

#### Tcam2base填在哪里

- **主文件：** `grasp_zy_zhiyuan1215.py` 第66-71行
- **备用文件：** `config.py` 第4-8行
- **建议：** 两处都改成相同的值

#### 简化步骤

手眼标定是一个专业操作，建议由技术人员完成。现场操作人员只需知道以下要点：

1. **什么情况必须重新标定：** 相机被移动过、重新安装过、换了支架、或者被撞过
2. **标定后怎么验证：** 在货箱中央放一个物品，看机械臂能否准确抓起来。如果抓取点偏了超过2厘米，说明标定需要重新做
3. **标定工具：** 项目中的 `calibration_gui.py` 提供了图形化标定界面，或联系技术支持协助完成

### 6.3 不需要重新标定的情况

以下情况**不需要**重新做标定：

1. 机械臂断电重启（机械臂基座坐标系没变）
2. 开发板重启或重装软件（Tcam2base值还在文件里）
3. 更换被抓取的物品种类（只要物品在相机视野内，位置关系没变）
4. 调整机械臂的运动速度
5. 修改网络IP地址

---

## 第7章 系统启停与CTU通信

### 7.1 首次启动6步流程

首次在新现场部署时，按以下步骤启动系统：

**第1步：检查硬件上电状态**

1. 确认机械臂控制器已上电，指示灯绿色常亮
2. 确认开发板已开机，可以通过SSH登录
3. 确认相机USB线已插好
4. 确认CTU小车已上电，MOXA已联网

**第2步：检查网络连通性**

在开发板终端执行：

```bash
ping 192.168.127.101   # 测试机械臂
ping 192.168.127.253   # 测试CTU/MOXA
```

**第3步：确认项目文件完整**

```bash
cd /home/jet/zoneyung/grasp_static
ls
# 应该能看到 grasp_zy_zhiyuan1215.py、ctu_conn.py、config.py 等文件
```

**第4步：手动试运行**

```bash
conda activate zy_torch
python ctu_conn.py
```

观察终端输出，确认：
- 没有红色的错误信息
- 机械臂回零到初始姿态
- 显示"连接成功"

按 `Ctrl+C` 停止手动运行。

**第5步：安装系统服务**

```bash
sudo cp /home/jet/zoneyung/grasp_static/RunGraspd.service /etc/systemd/system/RunGraspd.service
sudo systemctl daemon-reload
sudo systemctl enable RunGraspd.service
sudo systemctl start RunGraspd.service
```

**第6步：验证服务状态**

```bash
sudo systemctl status RunGraspd.service
```

如果看到 `Active: active (running)`，说明服务正常运行。

### 7.2 日常启停命令

首次部署完成后，日常操作只需要以下几条命令：

| 操作 | 命令 | 作用 |
|------|------|------|
| 启动服务 | `sudo systemctl start RunGraspd.service` | 启动抓取系统 |
| 停止服务 | `sudo systemctl stop RunGraspd.service` | 停止抓取系统 |
| 重启服务 | `sudo systemctl restart RunGraspd.service` | 重启抓取系统（修改配置后使用） |
| 查看状态 | `sudo systemctl status RunGraspd.service` | 查看服务是否正在运行 |
| 实时看日志 | `sudo journalctl -u RunGraspd.service -f` | 实时查看系统运行日志 |
| 设置开机自启 | `sudo systemctl enable RunGraspd.service` | 开机自动启动 |
| 取消开机自启 | `sudo systemctl disable RunGraspd.service` | 开机不自动启动 |

**日常启动流程：**

1. 给机械臂上电
2. 给开发板上电（如果设置了开机自启，等待约90秒后服务会自动启动）
3. 执行 `sudo systemctl status RunGraspd.service` 确认服务已启动
4. CTU小车开始正常工作

**日常停止流程：**

1. 执行 `sudo systemctl stop RunGraspd.service`
2. 机械臂会停在当前位置（不会自动回零）
3. 关闭机械臂电源
4. 关闭开发板电源

### 7.3 CTU通信流程通俗解释

系统启动后，`ctu_conn.py` 会持续与CTU小车保持通信。通信流程如下：

```
CTU小车                          开发板（本系统）
   |                                   |
   |--------- 1. 系统初始化 ----------->|
   |          （CTU上电后发送）         |
   |                                   |
   |<-------- 2. GRASP_OK -------------|
   |         （报告：机械臂已就绪）      |
   |                                   |
   |--------- 3. 心跳包 -------------->|
   |<-------- 4. 心跳包回应 -----------|
   |       （每10秒一次，保持连接）      |
   |                                   |
   |--------- 5. CTU_GRASP_START ----->|
   |         "开始分拣第X类物品"        |
   |                                   |
   |<-------- 6. GRASP_COUNT ----------|
   |         "发现N个目标物品"          |
   |                                   |
   |<-------- 7. GRASP_START --------->|
   |         "开始第1次抓取"            |
   |                                   |
   |<-------- 8. GRASP_START --------->|
   |         "开始第2次抓取"            |
   |         ...重复直到抓完...         |
   |                                   |
   |<-------- 9. GRASP_OVER -----------|
   |         "全部抓完了，回零待命"      |
```

**通俗解释：**

1. **GRASP_OK：** 开发板告诉小车"我准备好了"
2. **心跳：** 就像两个人打电话时定期说"还在吗"，确保连接没断
3. **CTU_GRASP_START：** 小车说"到工位了，开始分拣第3类物品"
4. **GRASP_COUNT：** 开发板回答"我看到了5个，准备开始抓"
5. **GRASP_START（循环）：** 每抓完一个就汇报一次"又抓了一个"
6. **GRASP_OVER：** 开发板说"5个全抓完了，我回初始位置待命了"

### 7.4 手动单步测试（wifi Rb_Pick命令）

如果CTU与机械臂通信异常，可以用CTU的WiFi命令进行单步测试：

**前提条件：**

1. 确保CTU小车WiFi已打开：

```bash
wifi wifi_enable 1
```

2. 调整CTU货叉到合适高度（货叉上的货箱应处于机械臂可抓取范围内）

**测试命令：**

```bash
wifi Rb_Pick 1 30
```

**命令格式说明：**

- `Rb_Pick`：手动触发一次抓取
- `1`：抓取第1类物品（对应GoogsMapping中的"1"）
- `30`：机械臂速度设为30

**预期结果：**

- 机械臂开始运动，执行一次完整的"识别→抓取→放置→回零"流程
- 如果机械臂正常动作，说明小车与机械臂通信链路正常
- 如果机械臂没反应，请检查MOXA配置和网络连接（详见第9章故障排查）

---

## 第8章 物品类别扩展

### 8.1 当前5类物品表格

系统当前已配置的物品类别如下：

| 类别编号 | 英文名称 | 中文名称 | 说明 |
|---------|---------|---------|------|
| 1 | daikon | 白萝卜 | 长条形白色根茎类蔬菜 |
| 2 | interrupter | 空气开关 | 白色，小型电气元件 |
| 3 | terminal | 接线端子 | 黑色，小型电气元件 |
| 4 | limit | 限位开关 | 小型机械/电气元件 |
| 5 | voltage | 电压采集模块 | 小型电子模块 |

**类别编号在哪里定义：**

- **文件：** `ctu_conn.py`
- **行号：** 第9-22行

```python
GoogsMapping = {
    "1": "daikon",       # 白萝卜
    "2": "interrupter",  # 空气开关
    "3": "terminal",     # 接线端子
    "4": "limit",        # 限位开关
    "5": "voltage"       # 电压采集模块
}
```

### 8.2 添加新类别的6步流程

如果要让系统识别和抓取新的物品，需要按以下步骤操作：

**第1步：准备训练数据**

1. 用相机拍摄新物品的照片（至少100张，不同角度、不同光照）
2. 用标注工具（如LabelMe、LabelImg）在每张图上标注物品的位置和轮廓
3. 导出为COCO格式或MMDetection支持的格式

**第2步：训练检测模型**

1. 基于现有的 `myconfig_zy.py` 配置文件，修改其中的类别数量和类别名称
2. 使用MMDetection框架训练新的检测模型
3. 训练完成后，得到新的权重文件（如 `epoch_XX_last.pth`）

**第3步：训练抓取模型**

1. 采集新物品的抓取数据（抓取点标注）
2. 训练或微调抓取点生成模型
3. 得到新的抓取模型权重文件

**第4步：更新物品映射**

打开 `ctu_conn.py`，在 `GoogsMapping` 中添加新类别：

```python
GoogsMapping = {
    "1": "daikon",
    "2": "interrupter",
    "3": "terminal",
    "4": "limit",
    "5": "voltage",
    "6": "new_item"    # 新增第6类
}
```

**第5步：更新模型路径**

打开 `grasp_zy_zhiyuan1215.py`：

- 第84行：更新抓取模型权重路径
- 第86行：更新检测模型配置文件路径（如有变化）
- 第87行：更新检测模型权重路径

**第6步：验证测试**

1. 重启 `RunGraspd.service`
2. 执行手动单步测试：`wifi Rb_Pick 6 30`
3. 观察新物品是否能被正确识别和抓取

### 8.3 GoogsMapping格式说明

`GoogsMapping` 是一个"编号到名称"的对照表，格式要求：

1. **键（左边引号里的数字）：** 必须是字符串形式，如 `"1"`、`"2"`。这是CTU小车发送的类别代码
2. **值（右边引号里的英文）：** 必须与检测模型训练时使用的类别名称完全一致，区分大小写
3. **编号顺序：** 可以不连续，但建议按1、2、3...顺序排列
4. **注释：** 以 `#` 开头的行是注释，不影响程序运行

**示例：**

```python
GoogsMapping = {
    "1": "daikon",
    # "1": "soap",   # 这一行被注释掉了，不会生效
}
```

---

## 第9章 故障排查

### 9.1 常见问题排查表

| 序号 | 现象 | 可能原因 | 解决方法 |
|------|------|---------|---------|
| 1 | 开发板ping不通机械臂 | 网线没插好、IP地址错误 | 检查网线；确认 `grasp_zy_zhiyuan1215.py` 第46行IP正确 |
| 2 | 开发板ping不通CTU | MOXA没通电、网线松动 | 检查MOXA电源和网线；确认 `ctu_conn.py` 第26行IP正确 |
| 3 | 服务启动后立即退出 | Python路径错误、模型文件缺失 | 检查 `RunGraspd.service` 中的路径；确认 `models/` 目录存在 |
| 4 | 机械臂不动，日志显示"逆解失败" | 物品位置超出可达范围 | 调整物品摆放位置；检查Z轴高度限制 `config.py` 第36行 |
| 5 | 相机画面全黑或绿屏 | USB线没插好、USB 2.0口 | 换USB 3.0口；重启开发板 |
| 6 | 能识别物品但抓偏了 | Tcam2base标定不准 | 重新执行第6章手眼标定 |
| 7 | 夹爪不张开/不闭合 | 夹爪供电异常、Modbus地址错误 | 检查 `gripper_zhiyuan.py` 第10-14行配置；检查夹爪接线 |
| 8 | 抓取时碰撞报警 | 碰撞等级太高、位姿路径不合理 | 调低碰撞等级（`grasp_zy_zhiyuan1215.py` 第47行）；调整位姿参数 |
| 9 | CTU不发送分拣指令 | MOXA配置错误、CTU主控未就绪 | 检查MOXA工作模式和端口；参考 `CTUArm.md` 重新配置MOXA |

### 9.2 碰撞恢复操作

#### 自动恢复

当机械臂在运动中检测到碰撞时，系统会自动执行以下恢复流程：

1. 立即停止当前运动
2. 临时降低碰撞灵敏度（从5降到2）
3. 机械臂先退回到 `mid_pose1` 位置
4. 再退回到 `init_pose` 初始位置
5. 恢复碰撞灵敏度到5
6. 继续下一次抓取尝试

**注意：** 同一批次最多重试2次，如果连续2次都碰撞，系统会放弃当前物品，继续抓下一个。

#### 手动恢复

如果自动恢复失败，或者机械臂卡在某个奇怪的位置，执行以下手动恢复步骤：

1. **执行急停：** 找到机械臂控制器上的急停按钮，按下去（或发送急停指令）
2. **手动拖动归位：** 按住机械臂底座上的**绿色按钮**（使能按钮），同时用手轻轻转动机械臂各关节，将其大致摆回初始姿态的形状
3. **重启服务：** 执行 `sudo systemctl restart RunGraspd.service`
4. **观察回零：** 服务启动后，机械臂会自动回到 `init_pose` 初始姿态

**参考图：** 机械臂底座上有一个绿色圆形按钮，按住它时机械臂处于"无力矩"状态，可以手动推动。

### 9.3 日志查看指南

查看系统日志是排查问题的最有效方法：

```bash
sudo journalctl -u RunGraspd.service -f
```

常用日志含义：

| 日志内容 | 含义 | 处理建议 |
|---------|------|---------|
| `连接成功` | 与CTU/MOXA通信正常 | 无需处理 |
| `接收到数据` | CTU发来了指令 | 正常通信中 |
| `开始抓取物品流程: xxx` | 收到分拣指令，开始工作 | 正常流程 |
| `待抓取物品[xxx]数量: N` | AI识别到N个目标物品 | 正常流程 |
| `机械臂逆解失败！` | 系统算不出机械臂怎么动 | 检查物品是否在可达范围内 |
| `碰撞检测到: xxx` | 机械臂运动中碰到了障碍物 | 检查周围是否有异物；降低速度 |
| `✓ 抓取成功完成！` | 单次抓取成功 | 正常流程 |
| `✗ 连续碰撞，达到最大尝试次数` | 同一物品多次碰撞失败 | 检查该物品是否被卡住或位置太偏 |

---

## 附录A 配置文件速查表

| 配置项 | 文件路径 | 关键行号 | 备注 |
|--------|---------|---------|------|
| 机械臂IP | `grasp_zy_zhiyuan1215.py` | 46 | `"192.168.127.101"` |
| 碰撞等级 | `grasp_zy_zhiyuan1215.py` | 47 | `5`（0-8） |
| 机械臂速度 | `grasp_zy_zhiyuan1215.py` | 39 | `30`（0-50） |
| init_pose | `grasp_zy_zhiyuan1215.py` | 56 | 初始姿态 |
| mid_pose | `grasp_zy_zhiyuan1215.py` | 57 | 中间过渡姿态 |
| mid_pose1 | `grasp_zy_zhiyuan1215.py` | 58 | 抓取准备姿态 |
| lift2init_pose | `grasp_zy_zhiyuan1215.py` | 61 | 回零路径姿态 |
| place_mid_pose | `grasp_zy_zhiyuan1215.py` | 62 | 放置路径姿态1 |
| place_mid_pose2 | `grasp_zy_zhiyuan1215.py` | 63 | 放置路径姿态2 |
| place_last_pose | `grasp_zy_zhiyuan1215.py` | 64 | 最终放置姿态 |
| place_last_pose（备用） | `config.py` | 1 | `[77,-104,38,-2,9,1]` |
| Tcam2base | `grasp_zy_zhiyuan1215.py` | 66-71 | 4x4变换矩阵 |
| Tcam2base（备用） | `config.py` | 4-8 | 4x4变换矩阵 |
| 基因描述文件 | `grasp_zy_zhiyuan1215.py` | 76 | `doc/single_new.txt` |
| 抓取模型权重 | `grasp_zy_zhiyuan1215.py` | 84 | `models/test_250927...` |
| 检测模型配置 | `grasp_zy_zhiyuan1215.py` | 86 | `models/mmdetection/...` |
| 检测模型权重 | `grasp_zy_zhiyuan1215.py` | 87 | `models/weights/...` |
| t_tcp_flange | `grasp_zy_zhiyuan1215.py` | 296 | `[0,0,0.2]` |
| tcp_compensate | `grasp_zy_zhiyuan1215.py` | 297 | `[0,0,0.018]` |
| slope_angle | `grasp_zy_zhiyuan1215.py` | 300 | `np.pi/8` |
| 边缘范围 | `grasp_zy_zhiyuan1215.py` | 301-303 | 左80,右480,上120,下292 |
| 图像裁剪 | `grasp_zy_zhiyuan1215.py` | 385-386 | `[:,80:560]` |
| CTU IP | `ctu_conn.py` | 26, 163 | `"192.168.127.253"` |
| CTU端口 | `ctu_conn.py` | 26 | `8899` |
| GoogsMapping | `ctu_conn.py` | 9-22 | 5类物品映射 |
| 最大抓取次数 | `ctu_conn.py` | 116 | `count + 5` |
| 最大逆解失败 | `ctu_conn.py` | 120 | `3` |
| 相机内参 | `camera.py` | 38-42 | fx,fy,cx,cy |
| Z轴高度 | `config.py` | 36-37 | `pose2=0.540` |
| robot_speed | `config.py` | 25, 34 | `20`（备用） |
| 夹爪初始化 | `gripper_zhiyuan.py` | 10-14 | Modbus地址0x0A |
| 服务配置 | `RunGraspd.service` | 全文件 | systemd服务 |
| 启动脚本 | `RunGrasp.sh` | 全文件 | zsh启动脚本 |

---

## 附录B 设备IP速查表

| 设备 | 有线IP | 无线IP | 端口 | 用户名 | 密码 |
|------|--------|--------|------|--------|------|
| Jetson开发板 | 192.168.127.102 | 192.168.2.51 | SSH: 33322 | jet | （空格） |
| RM65机械臂 | 192.168.127.101 | - | 8080 | - | - |
| CTU/MOXA | 192.168.127.253 | - | 8899 | admin | moxa |

**SSH登录开发板：**

```bash
ssh jet@192.168.127.102
# 密码：按空格键即可（密码是一个空格字符）
```

---

## 附录C 相关文档索引

本文档已覆盖日常操作所需的全部内容。如需了解更多技术细节，可参考项目目录下的以下文档：

| 文档路径 | 内容 | 适用读者 |
|---------|------|---------|
| `CTUArm.md` | CTU小车操作步骤、MOXA配置图文说明、机械臂复位流程 | 现场操作人员 |
| `ctu_protocol.md` | CTU与开发板之间的通信协议规范 | 技术开发人员 |
| `doc/RunGraspd.service_manual.md` | 系统服务的详细配置说明、常见问题排查 | 系统管理员 |
| `requirements.txt` | Python依赖包完整列表 | 技术开发人员 |
| `RunGraspd.service` | systemd服务配置文件模板 | 系统管理员 |
| `RunGrasp.sh` | 手动启动脚本（zsh） | 现场调试人员 |

**本文档不覆盖的内容：**

- GUI可视化界面（`grasp_gui_v2.py`）的操作说明
- AI模型训练方法和数据集制作
- CTU小车底盘的SLAM导航配置
- 机械臂SDK的底层API开发

---

> **文档版本：** v1.0
> **适用系统：** RM65 机械臂视觉抓取系统
> **最后更新：** 2026年4月
> **编写说明：** 本文档所有参数行号均经过代码验证，可直接对照使用。
